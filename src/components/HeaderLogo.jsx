import React from 'react'

export const HeaderLogo = ({title}) => {
    return (
        <div className="flex lg:flex-1">
            <a href="#" className="-m-1.5 p-1.5 title">{title}</a>
        </div>)
}
